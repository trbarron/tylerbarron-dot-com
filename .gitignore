node_modules

/.cache
/build
/dist
.env
.env.*
!.env.example
/public/build
/public/assets
/public/static.json
/app/posts/compiled
/server/*
!/server/package.json

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Testing
/coverage
/test-results
/playwright-report
/.eslintcache

# OS
.DS_Store
Thumbs.db
*.swp
*.swo

# Backup files
*.bak
*.backup
*.tmp
*~

# Claude Code: the shared config and skills are checked in; only personal
# machine-local overrides are ignored. docs/ is tracked in full.
.claude/settings.local.json

# spec-kit: templates and scripts are checked in; the generated constitution is
# not (regenerate with /speckit.constitution when it's needed again).
.specify/memory/

# Local databases. The Places scrape behind the pizza map lives outside the
# repo on purpose — see the pizza-map bullet in CLAUDE.md.
*.db
*.db-journal
*.db-wal
*.sqlite
*.sqlite-journal
*.sqlite-wal
